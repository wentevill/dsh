# DSH Mail Plugin

Production mail bundle for DeepSeek Harness. Install the packed archive with one native DSH command:

```sh
dsh plugin --profile web add ./dsh-mail-0.2.0.tgz
```

The package owns its IMAP, MIME and SMTP dependencies. DSH capabilities are peer dependencies supplied by the Desktop runtime.
